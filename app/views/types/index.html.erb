<p id="notice"><%= notice %></p>

<h1 class="text-center my-4">Tipos de archvio</h1>

<table class="table table-hover">
  <thead class="bg-info text-white">
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th colspan="3" class="text-center">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @types.each do |type| %>
      <tr>
        <td> <%= type.id %> </td>
        <td><%= type.name %></td>
        <td><%= link_to "Ver", type %></td>
        <td><%= link_to 'Editar', edit_type_path(type) %></td>
        <td><%= link_to 'Eliminar', type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3 class="text-center my-4">Grafico cantidad de marcadores segun el id al tipo que pertenece</h3>
<%= pie_chart @bookmarks.group(:type_id).count %>

<%= link_to "Volver atras", root_path, class: "btn btn-info" %>
<%= link_to 'Crear nuevo tipo', new_type_path, class: "btn btn-info" %>
