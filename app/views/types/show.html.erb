<p id="notice"><%= notice %></p>

<div class="jumbotron bg-info text-white text-center">
  <p>
    <strong>Name:</strong>
    <%= @type.name %>
  </p>

  <%= link_to 'Edit', edit_type_path(@type), class: "btn btn-light" %> |
  <%= link_to 'Back', types_path, class: "btn btn-light" %>
</div>

