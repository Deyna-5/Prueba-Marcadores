<%= form_with(model: bookmarker, local: true) do |form| %>
  <% if bookmarker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmarker.errors.count, "error") %> prohibited this bookmarker from being saved:</h2>

      <ul>
      <% bookmarker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :type_id %>
    <%= form.text_field :type_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
