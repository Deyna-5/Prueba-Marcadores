<%= form_with(model: @bookmark, remote: true, data: {confirm: "¿Deseas crear este marcador?"}, class: "form-inline" ) do |form| %>
  
  <% if @bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmark.errors.count, "error") %> prohibited this bookmark from being saved:</h2>
      <ul>
      <% @bookmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field mx-2">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>
  <div class="field mx-2">
    <%= form.label :type_id %>
    <%= form.collection_select :type_id, @types, :id, :name, prompt: "Selecciona el tipo" %>
  </div>
  <div class="field mx-2">
    <%= form.label :category_id %>
    <%= form.collection_select :category_id, @categories, :id, :name, prompt: "Selecciona la categoria" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
